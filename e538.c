#include<stdio.h>
#include<stdlib.h>
int main (void)
{
	int n,d,r;
	while(scanf("%d %d %d",n,d,r)==3){
		if(n==d==r==0){
			break;
		}
		int i,j,money;
		int early[n],late[n];
		else if{
			for(i=0;i<n;i++){
				if(early[i]+late[i]>d){
					money=(early[i]+late[i])*r;
				}
			}
		}
	}
	return 0;
} 

